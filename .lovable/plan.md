Increase the logo size in the top-left header so it is much more prominent.

- In `src/components/site/SiteHeader.tsx`, change the logo image classes from `h-14 w-auto md:h-18` to `h-16 w-auto md:h-22`.
- Increase the header container height from `h-24` to `h-28` to keep the logo vertically centered and avoid crowding the nav/booking button.
- Leave all nav labels, links, and the booking CTA unchanged.